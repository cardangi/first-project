{% for item in list2|sortedlist() %}
    {% if loop.first %}


    {% endif %}

          {{ loop.index|integertostring()|rjustify(3) }}. {{ item[0] }}
            {% if loop.length > 1 %}
                {% for i in range(1, loop.length)
               {{ item[i] }}
                {% endfor %}
           {% endif %}
    {% if loop.last %}


          {{ loop.length|integertostring()|rjustify(3) }} file(s) ready to be {{ mode }}.
    {% endif %}
{% endfor %}
