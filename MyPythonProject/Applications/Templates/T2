{% extends "T1" %}

{% block body1 %}

     STEP {{ step|integertostring()|rjustify(width=2) }}. {{ title }}
{% endblock %}

{% block body2 %}
    {% if information %}

        {% for inf in information %}
     {{ inf }}
        {% endfor %}
    {% endif %}
{% endblock %}
